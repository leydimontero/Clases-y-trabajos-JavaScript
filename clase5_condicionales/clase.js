let lenguaje = "javascript";

if(lenguaje === "Python"){
    lenguaje = "Estoy aprendiendo"
    } else {
    lenguaje = "lo aprenderé más adelante"
}
console.log(lenguaje);

for(let vuelta = 1; vuelta <= 5; vuelta++){
    console.log('Dando la vuelta númeron ' + vuelta);
}

function loro(texto){
    for(texto = 1; texto <= 5; texto++){
        console.log('Hola munddo ' + texto);
    }
}
loro(1);